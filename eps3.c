#include <stdio.h>

int main(){
  int x, b, c, d; // int adalah sebuah tipe data yang memiliki nilai berupa angka
  float y; // float adalah tipe data yang memiliki nilai berupa angka (angkanya desimal ada koma (.))
  char z, l, q; // char adalah karakter
  // double
  // long
  // short
  /* cara peng-outputan tipe data int dengan %d
     cara peng-outputan tipe data float dengan %f
     cara peng-outputan tipe data char dengan %c
  */
//  ini untuk int
  // x = 10;
  // b = 25;
  
  // printf("x = %d \na = %d", x, b);

  // ini untuk float
  // y = 2.3;
  // printf("y = %.1f \n", y);
  // printf("y = %.3f \n", y);
  // printf("y = %f \n", y);

  //ini untuk char
  z = 'y';
  l = 'd';
  q = 'x';
  printf("%d", q);


  return 0;
}