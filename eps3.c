#include <stdio.h>

int main(){
  int x; // int adalah sebuah tipe data yang memiliki nilai berupa angka
  float y; // float adalah tipe data yang memiliki nilai berupa angka (angkanya desimal ada koma (.))
  char z; // char adalah karakter
  /* cara peng-outputan tipe data int dengan %d
     cara peng-outputan tipe data float dengan %f
     cara peng-outputan tipe data char dengan %c
  */
  x = 10;
  printf("x = %d", x);

  return 0;
}